// Ficheiro: src/components/maps/MapMarker.tsx | Função: stub (react-native-maps removido temporariamente)
// O componente real será restaurado quando o ecrã de mapa for migrado para uma stack sem GMS.
import React from 'react';

interface Props {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  [key: string]: any;
}

export default function MapMarker(_: Props): React.ReactNode {
  return null;
}
